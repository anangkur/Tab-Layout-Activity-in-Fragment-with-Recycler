<template format="4"
        revision="1"
        name="Tab Layout in Fragment Activity with recyclerview"
        description="Creates a new Tab activity within fragment classes with recyclerview">

    <category value="Other"/>

    <parameter
        id="mainActivityClass"
        name="Activity Name"
        type="string"
        constraints="class|unique|nonempty"
        suggest="${layoutToActivity(mainLayout)}"
        default="MainActivity"
        help="The name of the activity class to create" />

    <parameter
        id="mainActivityLayout"
        name="Layout Name"
        type="string"
        constraints="layout|unique|nonempty"
        suggest="${activityToLayout(mainActivityName)}"
        default="activity_main"
        help="The name of the layout to create for the activity" />

    <parameter
        id="mainFragmentClass"
        name="Fragment Name"
        type="string"
        constraints="class|unique|nonempty"
        default="MainFragment"
        help="The name of the fragment class to create" />

    <parameter
        id="mainFragmentLayout"
        name="Fragment Layout Name"
        type="string"
        constraints="layout|unique|nonempty"
        default="fragment_main"
        help="The name of the layout to create for the activity" />

    <parameter
        id="tabFragmentClass"
        name="Fragment Name"
        type="string"
        constraints="class|unique|nonempty"
        default="TabFragment"
        help="The name of the fragment class to create" />

    <parameter
        id="tabFragmentLayout"
        name="Tab Fragment Layout Name"
        type="string"
        constraints="layout|unique|nonempty"
        default="fragment_tab"
        help="The name of the layout to create for the activity" />

    <parameter
        id="itemAdapterClass"
        name="Item Adapter Name"
        type="string"
        constraints="class|unique|nonempty"
        default="MainAdapter"
        help="The name of the adapter class" />

    <parameter
        id="itemLayout"
        name="item Layout Name"
        type="string"
        constraints="layout|unique|nonempty"
        default="item_main"
        help="The name of the item layout" />

    <parameter
        id="viewModelClass"
        name="View model Name"
        type="string"
        constraints="class|unique|nonempty"
        default="MainViewModel"
        help="The name of the view model class" />

    <parameter
        id="packageName"
        name="Package name"
        type="string"
        constraints="package"
        default="com.mycompany.myapp" />

    <execute file="recipe.xml.ftl" />

</template>